# lm_principle Wiki 索引

这套 wiki 解决三个问题：

1. 新 session 老失忆
2. 资料散在各处，没人知道先看哪
3. 新结论只活在聊天里

默认范式：

- `compile-first`
- `writeback` 必做
- 中等规模先 `wiki`
- Obsidian 可替换，范式不可替换
- `Idea / Intent` 优先于 `Code`

自检：

```bash
python3 scripts/wiki_check.py
python3 scripts/raw_manifest_check.py
```
